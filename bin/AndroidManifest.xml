<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nest5.businessClient"
    android:installLocation="preferExternal"
    android:versionCode="1"
    android:versionName="0.1" >

    <uses-sdk android:minSdkVersion="15" />

    <supports-screens android:xlargeScreens="true" />

    <!-- Only this application can receive the messages and registration result -->
    <permission
        android:name="com.nest5.businesClient.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.nest5.businesClient.permission.C2D_MESSAGE" />

    <!-- This app has permission to register and receive message -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <!-- Permission to keep the processor from sleeping -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Permission to retrieve the list of accounts in the Accounts Service -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!-- Permission to request authtokens from the AccountManager -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <!-- Send the registration id to the server -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
     <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
     
     

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/Nest5.Default" >
        <uses-library android:name="com.google.android.maps" />

        <activity
            android:name="com.nest5.businessClient.Initialactivity"
            android:label="@string/app_name"
            android:screenOrientation="landscape" >
            
        </activity>

        <!--
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" > 
            
           
        </activity>
        -->

        <activity
            android:name="com.nest5.businessClient.DealsActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.nest5.businessClient.FacebookActivity"
            android:label="@string/app_name" >
        </activity>

        <!-- Account selection activity -->
        <activity
            android:name="com.nest5.businessClient.AccountsActivity"
            android:label="@string/app_name"
            android:screenOrientation="landscape" >
        </activity>

        <service
            android:name="com.nest5.businessClient.ExecuteRequest"
            android:label="ExecuteRequest" />

        <!--
             In order to use the C2DM library, an application must declare a class 
             with the name C2DMReceiver, extending com.google.android.c2dm.C2DMBaseReceiver.
             It must also include this section in the manifest.
        -->
        <service android:name="com.nest5.businessClient.C2DMReceiver" />

        <!--
             Only Google services can send data messages for the app. If permission
             is not set - any other app can generate it
        -->
        <receiver
            android:name="com.google.android.c2dm.C2DMBroadcastReceiver"
            android:permission="com.google.android.c2dm.permission.SEND" >

            <!-- Receive the actual message -->
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />

                <category android:name="com.nest5.androidClient" />
            </intent-filter>
            <!-- Receive the registration id -->
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />

                <category android:name="com.nest5.androidClient" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.nest5.businessClient.LoginActivity"
            android:label="@string/title_activity_login"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>